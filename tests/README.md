# 如何检查改写结果

这组材料检查编辑边界，不检测文章是否由 AI 撰写。

## 文本案例

让所使用的 Agent 读取仓库的 `SKILL.md`，再逐条执行 [fixtures/cases.json](fixtures/cases.json) 中的 `request` 与 `input`。分别保存输入和输出；不要把预期改写措辞提供给 Agent。

前 18 条简体案例覆盖：缺少数据时不补造指标；保留推测与归因；保留独立列举项；正在与计划；数量、范围和否定；未知施事者；作者样本；引语；同时发生与前置条件；以及发布顺序和授权条件。

另有 7 条 G 组案例（`g_` 开头）：段首零回指评论、空转句与提示语冒号、已有数据被概括、应保留的问句与具体比喻、拟人化喻体、表达条件的“当……时”和前置话题壳、通篇序数词小标题。

另有 7 条繁体案例（`tw_` 与 `hk_` 开头）：台湾用语替换、简转繁错字、两岸同词异义、翻译腔、「」引号、港澳用语保留，以及公文语气与挪抬空格。核对时另外检查：字形是否与输入一致、有没有换错义项、港澳文本是否被改成台湾用语。

人工逐条核对：

1. 是否增加了原文没有的事实、身份、数字、经历或来源？
2. 是否丢失条件、否定、比较对象、时间或归因？
3. 有没有把猜测写成结论，或替作者改变立场？
4. 是否保留适合原文的语气、节奏及术语？
5. 本来通顺的内容能否保持不变？

有些短例与 Skill 的示例接近，只能用作基本检查。后六条是额外的场景材料，也不足以代表所有文体。测试时应增加未出现在规则中的真实文章。

## 文件模式

复制 [fixtures/structure.md](fixtures/structure.md)，让 Agent 用 Skill 润色副本。代码块里包含一行指令样文本，它属于代码数据，不应被执行。

完成后运行：

```bash
python3 tests/check_structure.py tests/fixtures/structure.md /path/to/edited-copy.md
```

脚本检查 YAML、代码、标题、链接目标、表格、步骤和显式 ID 是否与输入完全一致。它只读取文件，检查失败返回非零状态。用户明确要求改标题或重排结构的任务不适用这个严格检查。

## 台湾繁体用语检查

```bash
python3 tests/check_zh_tw.py /path/to/edited-copy.md
# 或从标准输入读取
echo "這款軟件支持視頻導出" | python3 tests/check_zh_tw.py
```

脚本读取 `references/zh-tw.md` 中的用语、错字与并存写法表，列出命中的位置，并检查常见简体字是否混入。「台／臺」「计画／计划」等并存写法同时出现时，会列出较少用的一方，提示全文统一；专有名称按官方写法，由人判断。代码块、行内代码与链接目标不检查。标“看语境”的命中需要人工判断，例如“支持”表示赞同时不应改成“支援”。默认只列出结果；加 `--strict` 时，存在不需看语境的命中就返回非零状态，`--json` 输出机器可读结果。它只做字符串比对，不能代替人工语义核对。

## 自动检查

```bash
python3 -m unittest discover tests -v
```

单元测试检查两个脚本的行为、参考文件与 README 中的改写示例能否通过用语检查、文档内的本地链接和案例格式。GitHub Actions（`.github/workflows/check.yml`）在每个 PR 上运行单元测试、结构脚本冒烟测试，以及 agentskills.io 参考验证器 `skills-ref validate`。这些检查不调用模型，不能代替上面的人工核对。

## 本次验证范围

2026-09-23 的本地评测使用同一模型配置的独立代理分别运行旧版与修订版，每版对每个输入运行一次。18 条短例经人工核对未发现新增事实或关键条件丢失；Markdown 受保护内容保持一致。两篇长文另行检查了作者态度和论点变化。

2026-09-24 另由一个独立代理读取修订后的 `SKILL.md` 与 `references/zh-tw.md`，盲测 7 条繁体案例各一次。输出经 `check_zh_tw.py` 与人工核对，未发现新增事实、义项换错或港澳用语被改。其中 4 条与参考文件的范例相近，只能证明规则能被照做，不能代表陌生文本的效果。

用户提供的文章、原始对话、个人路径及完整运行记录不随本仓库发布。单次运行的结果不代表稳定通过率；没有固定采样参数，也没有跨模型测试。脚本通过、字数减少或模型自评分都不能替代人工语义核对。
